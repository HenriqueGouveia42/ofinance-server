Este diretório   utilizado para armazenar configurações globais ou específicas de integração, como:

Configuração de bancos de dados (db.js);

Serviços externos (ex.: provedores de envio de e-mail, APIs externas);

Variáveis de ambiente que precisam ser centralizadas.